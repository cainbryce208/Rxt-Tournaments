name: Build Vue
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: LizAinslie/VueBuildToBranchAction@1.0.2
      with:
        username: 'cainbryce208'
        reponame: 'Rxt-Tournaments'
        token: ${{ secrets.BRYCEPAC }} # Leave this line unchanged
        branch: 'deployment' # defaults to 'gh-pages' but can be changed
